装饰器可以分离关注度
装饰器的作用可以让类更专注自身的逻辑，而一些通用的功能则使用装饰器进行实现。
视频中举了一个校验的例子：
```typescript
class User {
    private userName: string;
    private userPassword: string;
    private userAge: number;
    constructor(userName: string, userPassword: string, userAge: number) {
        this.userName = userName;
        this.userPassword = userPassword;
        this.userAge = userAge;
    }
}
```

为需要对user传入的📄进行校验，例如name需要4-8个字符；password需要6-18个字符，且需要包含数字、字母；age为数字等等
其实可以通过内部定义校验函数的方式来解决，但是存在两个问题：
一、user内部需要完成很多逻辑内容，注册、登陆状态等，如果再加上校验逻辑 多少有些太过于分散了。
二、如果此时我再定一个article类
```typescript
class Artical {
    private title: string;
    private content: string;
    constructor(title: string, content: string) {
        this.title = title;
        this.content = content;
    }
}
```
我也需要对title进行长度限制呢？是否就写了重复的长度校验逻辑

其实很多UI库已经做了类似的思想，例如我可以直接用
```typescript
class User {
    @type("string")
    @require({ rule: length, min: 4, max: 8 })
    private userName: string;
    @type("string")
    @require({ rule: length, min: 6, max: 18 })
    private userPassword: string;
    @type("number")
    @content({ max: 200, min: 0})
    private userAge: number;
    constructor(userName: string, userPassword: string, userAge: number) {
        this.userName = userName;
        this.userPassword = userPassword;
        this.userAge = userAge;
    }
}
```

其实使用类+方法的方式也可以进行结果，但是装饰器有了更优雅的解决方案。
使用装饰器类似于接口的方式，装饰器可以不关注内部实现，只使用外部来使用
